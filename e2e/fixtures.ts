// Pinned ids for e2e specs. Picked from the current static export (`out/`)
// so tests stay stable as the underlying NBA dataset grows. Re-pin here if
// either id ever stops resolving (e.g. dataset gets pruned/renumbered).
export const FIXTURE_SEGMENT = "2025-26-regular-season";
export const FIXTURE_GAME_ID = "0022500001";
